<!DOCTYPE html>
<html lang="en">
<head>
    <title>Lista de Usuarios</title>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/userManagement/userList.css">
</head>
<body>
    <div class="container">
        <!-- HEADER -->
        <%- include('../partials/header') %>

        <!-- MAIN -->
        <main id="main">
            <% for(let i=0; i<users.length; i++){ %>
            <div class="user">
                <a href="/userManagement/<%= users[i].id %>"><img src="/images/users/<%= users[i].image %>" alt="User image" class="user-image"></a>
                <a href="/userManagement/<%= users[i].id %>" class="user-name"><%= users[i].first_name + ' ' + users[i].last_name %></a>
                
                <div class="user-buttons">
                    <a href="/userManagement/<%= users[i].id %>/edit" class="user-edit">
                        Editar Usuario
                    </a>
                    <form action="/userManagement/<%= users[i].id %>?_method=DELETE" method="POST">
                        <button type="submit" class="user-delete">
                            Borrar Usuario
                        </button>
                    </form>
                </div>
            </div>
            <% } %>
        </main>
        <a href="/userManagement/create" class="new-user">Crear un nuevo usuario</a>

        <!-- FOOTER -->
        <%- include('../partials/footer') %>
    </div>
</body>
</html>